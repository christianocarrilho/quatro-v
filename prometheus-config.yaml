global:
  scrape_interval: 5s # Coleta os números do Quarkus a cada 5 segundos

scrape_configs:
  - job_name: 'quatrov-quarkus-app'
    metrics_path: '/q/metrics' # Rota padrão de métricas do Quarkus
    static_configs:
      - targets: ['host.docker.internal:8080']